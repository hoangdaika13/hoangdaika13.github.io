# AI Super Platform Backend TODO

Nhung module can backend that truoc khi coi la san sang san xuat:

- `content-calendar`: can database cho lich dang bai, trang thai va reminder.
- `asset-library`: can storage upload file, metadata, preview va tag search.
- `crm-lite`: can database user/lead, pipeline va lich su cham soc.
- `task-autopilot`: can auth, database task, reminder queue va realtime sync neu lam nhom.
- `research-collector`: can database nguon, tag, citation va full-text search.
- `api-tester`: can luu request history, environment secrets va proxy/CORS an toan.
- `flashcard-builder`: can database tien do hoc va spaced repetition.
- `knowledge-base`: can database bai viet, permission va revision history.
- `automation-builder`: can webhook endpoint, scheduler, queue worker va action log.
- `email-campaign-lab`: can mail provider, contact list, unsubscribe va tracking.
- `notification-center`: can event bus, delivery queue, inbox va push/email provider.
- `webhook-inbox`: can public endpoint, payload log, filter va replay request.
- `analytics-dashboard`: can tracking endpoint, event storage va aggregation.
- `user-admin`: can auth, roles, sessions va audit log.
- `billing-admin`: can payment provider, subscription state va usage metering.

Ghi chu: GitHub Pages chi chay frontend tinh. Cac module tren nen noi vao backend rieng nhu Vercel/Render/Railway/Fly.io hoac VPS.
